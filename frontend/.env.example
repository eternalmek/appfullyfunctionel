# Backend API URL (defaults to localhost:4000 in development)
VITE_API_URL=http://localhost:4000

# Note: Supabase is used as the PostgreSQL database on the backend only.
# The frontend communicates with the backend API - no direct Supabase connection needed.
