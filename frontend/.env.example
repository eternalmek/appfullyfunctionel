# EternalMe Frontend Environment Variables
# Copy this file to .env and adjust as needed

# =============================================================================
# BACKEND API
# =============================================================================
# Backend API URL (defaults to localhost:4000 in development)
VITE_API_URL=http://localhost:4000

# =============================================================================
# NOTE ABOUT SOCIAL LOGIN & AI CHAT
# =============================================================================
# Social login (Instagram, Facebook, TikTok) and AI chat features are powered
# by the backend API. To enable these features:
#
# 1. Set up OAuth credentials for social platforms in the backend .env file
# 2. Set up an OpenAI API key in the backend .env file for AI chat
#
# See the backend .env.example for full configuration details.
#
# When OAuth is not configured, social login buttons will work in "demo mode"
# (simulating login without real authentication).
#
# When OpenAI is not configured, the Mirror chat will use intelligent
# rule-based fallback responses.

